# Pinned for reproducibility of the numbers in the paper.
# See Section 4 (Experimental Setup) for rationale.
# Versions are known-good on Ubuntu 22.04 (WSL2), CUDA 12.8, RTX 4080 Laptop 12 GB.

# Core: model loading, quantization pipeline, calibration I/O
torch==2.10.0
transformers==5.3.0
datasets==3.2.0
accelerate==1.1.1
huggingface_hub==0.26.2
safetensors==0.4.5

# Evaluation harnesses
lm-eval[api]==0.4.11

# Numeric / scientific
numpy==2.1.2
scipy==1.14.1
tqdm==4.66.4

# Plotting (fig_kurtosis_distribution, fig_routing_imbalance)
matplotlib==3.9.2

# fast-hadamard-transform and external/qtip/qtip-kernels are installed
# from source; see README.md (Install) for build instructions.
